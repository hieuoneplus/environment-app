server.port=${PORT:8080}

# Database - Use environment variables in production, fallback to local config for dev
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d50592ggjchc73d8oj2g-a.singapore-postgres.render.com:5432/environment_8es2}
spring.datasource.username=${DATABASE_USERNAME:environment}
spring.datasource.password=${DATABASE_PASSWORD:b1ZG1otMAGu551mfR07n16W3WlgBU2Bk}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.default_schema=public
spring.datasource.hikari.connection-init-sql=SET search_path TO public;
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=20000

# JPA/Hibernate settings
# Tạm thời set thành 'none' nếu gặp lỗi migration, sau khi chạy SQL script thì đổi lại thành 'update'
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
# Allow Hibernate to handle schema updates more gracefully
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false
spring.jackson.serialization.WRITE_DATE_TIMESTAMPS_AS_NANOSECONDS=false

